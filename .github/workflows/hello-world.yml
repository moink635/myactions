name: hello-world

on: push 
jobs: 
  hello-world-jobs: 
     runs-on: ubuntu-latest 
     steps: 
       - name: checkout repo code 
         uses: actions/checkout@v4
       - run: ehco "hello world print"  
